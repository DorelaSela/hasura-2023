import React from "react";
import { useParams } from "react-router-dom";

const AddRelations = () => {
//   const { id } = useParams();
//   console.log(id);
  return (
    <div>
      <h2>Add Relations</h2>
    </div>
  );
};

export default AddRelations;
